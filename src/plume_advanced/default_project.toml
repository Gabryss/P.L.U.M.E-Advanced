# Edit this recipe. Shared calibration is resolved in memory; no second file is modified.
recipe_version = 1
preset = "preview"  # short-single, short-multi, long-single, long-multi; see README
procedural_seed = 3

[world]
body = "earth"  # earth, mars, moon (use body-study for body-scaled domains)

[run]
render_diagnostics = true

[export]
target = "neutral"  # blender, unity, ue5, gazebo, omniverse, all
